# SMTP configuration - set these in your deployment or local .env
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
# Use secure connection? set to "true" for port 465
SMTP_SECURE=false
# Destination email for contact form submissions
DEST_EMAIL=middris112@gmail.com
